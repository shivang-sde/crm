# Database (used by docker-compose and optional local overrides)
POSTGRES_USER=
POSTGRES_PASSWORD=
DATABASE_URL=

# RabbitMQ
RABBITMQ_DEFAULT_USER=
RABBITMQ_DEFAULT_PASS=

# Integration credential encryption (Base64-encoded 32-byte AES-256 key)
INTEGRATION_ENCRYPTION_KEY=""
password_hash=""

# JWT signing secret (Base64-encoded)
JWT_SECRET=

# CORS
CORS_ALLOWED_ORIGINS=
